<!--{#if post}}-->
    <!--<div>-->
        <!--<form action="/edit/thought/{post.id}}" method="post">-->
            <!--&lt;!&ndash;<input type="hidden" placeholder="{post.id}}" id="{post.id}}">&ndash;&gt;-->
            <!--<div class="form-group">-->
                <!--<label for="thought-title-input">Title</label>-->
                <!--<input class="form-control" type="text" value="{post.post_title}}" id="thought-title-input">-->
            <!--</div>-->
            <!--<div class="form-group">-->
                <!--<label for="thought-body-input">Comment</label>-->
                <!--<textarea class="form-conrol" rows="8" id="thought-body-input">{post.post_body}}</textarea>-->
            <!--</div>-->
            <!--<button type="submit" class="btn btn-dark btn-outline-dark update-btn">UPDATE</button>-->
        <!--</form>-->
    <!--</div>-->
<!--{/if}}-->


<div>
    <form action="/edit/thought/{{post.id}}" method="post">
        <div class="form-group">
            <label for="thought-title-input">Title</label>
            <input class="form-control" type="text" value="{{post.post_title}}" id="thought-title-input" name="postTitle">
        </div>
        <div class="form-group">
            <label for="thought-body-input">Comment</label>
            <textarea class="form-control" rows=8 id="thought-body-input" name="postBody">{{post.post_body}}</textarea>
        </div>
        <button type="submit" class="btn btn-outline-dark btn-dark update-btn">SUBMIT</button>
    </form>
</div>
